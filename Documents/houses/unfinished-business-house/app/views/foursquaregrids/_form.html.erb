<%= form_for(@foursquaregrid) do |f| %>
  <% if @foursquaregrid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@foursquaregrid.errors.count, "error") %> prohibited this foursquaregrid from being saved:</h2>

      <ul>
      <% @foursquaregrid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </div>
  <div class="field">
    <%= f.label :picture %><br>
    <%= f.text_field :picture %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
