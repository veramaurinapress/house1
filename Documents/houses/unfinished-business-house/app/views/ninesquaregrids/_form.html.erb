<%= form_for(@ninesquaregrid) do |f| %>
  <% if @ninesquaregrid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ninesquaregrid.errors.count, "error") %> prohibited this ninesquaregrid from being saved:</h2>

      <ul>
      <% @ninesquaregrid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </div>
  <div class="field">
    <%= f.label :picture %><br>
    <%= f.text_field :picture %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
